@model IEnumerable<University_Registration.Models.Section>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SectionNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectionTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectionDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SectionNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionDay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subject.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Section_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Section_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Section_ID })
        </td>
    </tr>
}

</table>
